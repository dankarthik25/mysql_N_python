CREATE DATABASE dog_walking_app;
USE dog_walking_app;
SELECT DATABASE();

-- DATA TYPES
-- why different datatypes in sql ?
-- 

--  Different datatypes

-- Numeric Types (int, smallint,tinyint,mediumint,bigint,decimal,numeric,float,double,bit)
-- String Types (char, varchar, binary,varbinary, blob, tinyblob,mediumbloab,longblob,text,tinytext,mediumtext,longtext,enum)
-- Data Type (Data, Datatime, timestamp,time,year)

-- general use data type ( int,  varchar)